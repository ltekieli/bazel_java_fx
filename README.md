# Build OpenJFX with Bazel

Example of building an OpenJFX application with Bazel.

# Prerequisites
Install [Bazelisk](https://github.com/bazelbuild/bazelisk) for automatic Bazel (https://bazel.build/) installation management

# Build
```
$ bazel build //...
```

# Run
```
$ bazel run //...
```
